'use strict';

contract('FlightFuture.Marked', (accounts) => {
   it('should transition to the verified state and then the marked state if markToMarket transaction made while state ' +
       'is Accepted and _shouldBuy returns false', (done) => {
       Promise.coroutine(function*() {
            return done();
       })().catch(done);
   });

   it('should transition to the verified state and then ')
});